export const APP_ROUTES = {
  HOME: '/',
  MOVIES: '/movies',
  RATED: '/rated',
} as const;

export const API_BASE_URL = '/api';

export const API_ROUTES = {
  MOVIE: '/movie',
  MOVIES: '/movies',
  GENRES: '/genres',
} as const;
